%%%-------------------------------------------------------------------
%% @doc neptr public API
%% @end
%%%-------------------------------------------------------------------

-module(neptr_app).

-behaviour(application).

%% Application callbacks
-export([start/2, stop/1, sendMessage/1]).

%%====================================================================
%% API
%%====================================================================

start(_StartType, _StartArgs) ->
  neptr_sup:start_link().

%%--------------------------------------------------------------------
stop(_State) ->
  ok.

%%====================================================================
%% Internal functions
%%====================================================================

sendMessage(Message) ->
  neptr_utils:seedPID() ! {message, neptr_utils:peerPID(), Message}.
